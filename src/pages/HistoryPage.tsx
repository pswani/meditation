import PlaceholderScreen from '../components/PlaceholderScreen';

export default function HistoryPage() {
  return (
    <PlaceholderScreen
      title="History"
      description="Review session log history with clear manual log and automatic log context, plus summary entry points."
      highlights={['Session log timeline', 'Manual log and auto-log distinction', 'Summary filters and drill-downs']}
    />
  );
}
