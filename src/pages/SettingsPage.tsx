import PlaceholderScreen from '../components/PlaceholderScreen';

export default function SettingsPage() {
  return (
    <PlaceholderScreen
      title="Settings"
      description="Adjust defaults and preferences that support a steady, low-friction meditation routine."
      highlights={['Default duration and sounds', 'Session logging preferences', 'General app preferences']}
    />
  );
}
