import PlaceholderScreen from '../components/PlaceholderScreen';

export default function PracticePage() {
  return (
    <PlaceholderScreen
      title="Practice"
      description="Set up meditation sessions with timer controls, meditation type selection, custom plays, and playlists."
      highlights={['Timer setup', 'Meditation type selection', 'Custom play and playlist entry points']}
    />
  );
}
