# 99 Merge Branch

Finish the full `sankalpa` parity slice.

Steps:
1. Review changed files and confirm no unrelated edits were introduced.
2. Ensure durable docs and slice artifacts are updated.
3. Run final verification if anything changed after the last test pass.
4. Create a clear commit message, such as:
   - `feat(sankalpa): add recurring goal parity on ios`
   - or another accurate cross-platform message if the slice spans more than native
5. Merge using the repo’s normal non-interactive workflow.

Close-out summary should state:
- the final recurring-goal model
- how cumulative and observance goals remain compatible
- where parity was added
