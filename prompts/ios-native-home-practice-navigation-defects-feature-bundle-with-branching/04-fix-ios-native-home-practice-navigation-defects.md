# 04 Fix Native Home And Practice Navigation Defects

Use this prompt to address anything discovered in:

- `docs/review-ios-native-home-practice-navigation-defects-feature.md`
- `docs/test-ios-native-home-practice-navigation-defects-feature.md`

Fix rules:
- Keep the branch scoped to the Home, Practice, and `custom play` navigation defects in this bundle.
- Update the ExecPlan progress log with each substantive follow-up fix.
- Re-run the affected automated checks after each meaningful fix, and update the test doc with the final pass or residual failure state.
- Update `requirements/session-handoff.md` if the final behavior, remaining risks, or next-slice advice changed.

Do not merge from this prompt. When the branch is clean, recommend `99-merge-branch.md`.
