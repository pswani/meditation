# Group Closeout: Sankalpa Activity Gym Goal

Use the `docs-and-cleanup` reasoning profile.

## Tasks

- Confirm both bundle branches were merged into `codex/integration-sankalpa-activity-tracking-gym-goal`.
- Review changed files with `git status --short` and `git diff --stat`.
- Confirm no unrelated edits were introduced.
- Confirm durable docs reflect the final behavior and any long-lived decisions:
  - `README.md`
  - `docs/product-requirements.md`
  - `docs/architecture.md`
  - `docs/ux-spec.md`
  - `docs/screen-inventory.md`
  - `requirements/decisions.md`
  - `requirements/session-handoff.md`
- Summarize verification commands and outcomes.
- Leave these temporary prompt artifacts in place until the full pile is done, then remove them before merging to `main` unless the user explicitly asks to keep them.

Do not merge the integration branch to `main`.
