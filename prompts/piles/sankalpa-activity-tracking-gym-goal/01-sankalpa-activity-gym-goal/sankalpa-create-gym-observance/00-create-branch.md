# Create Branch: Sankalpa Create Gym Observance

Default parent branch: `codex/integration-sankalpa-activity-tracking-gym-goal`

Default feature branch: `codex/sankalpa-create-gym-observance`

## Steps

1. Check `git status --short --branch`.
2. Switch to the parent branch.
3. Pull or fetch only if the operator explicitly asks for remote updates.
4. Create or switch to `codex/sankalpa-create-gym-observance`.
5. Confirm the working tree is ready before implementation.

Do not touch unrelated product areas in this bundle.
