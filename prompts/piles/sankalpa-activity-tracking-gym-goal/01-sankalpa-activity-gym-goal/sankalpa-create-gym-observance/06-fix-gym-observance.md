# Fix: Gym Observance Bundle

Use this prompt only if review or verification finds issues.

## Fix Rules

- Keep fixes inside this bundle's Sankalpa model/editor scope.
- Do not widen into unrelated Goals summary, History, timer, playlist, or custom play work.
- Preserve existing sankalpas and local/backend compatibility.
- Add or update focused tests for each behavioral fix.
- Rerun the smallest failing command first, then the bundle verification from `05-test-gym-observance.md`.

Record any durable decision in `requirements/decisions.md` during group closeout if it remains true after the full group is complete.
